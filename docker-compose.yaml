version: '2'

services:
  chatapp:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        mongouri: mongodb+srv://vishnu:Mongodbatlas@cluster0.4wbpkmt.mongodb.net/ChatAppDB
    image: chatappmern
    ports:
    - 5000:5002
  nginx:
    image: nginx:latest
    ports:
    - 81:80
    volumes:
    - ./nginx.conf:/etc/nginx/nginx.conf
